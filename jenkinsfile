pipeline {
    agent { label 'yabusaki' }
    options { disableConcurrentBuilds() }

    stages {
        stage('Test') {
            steps { sh "python --help" }
        }
        stage('Test') {
            steps { sh "pytest --help" }
        }
    }
}
